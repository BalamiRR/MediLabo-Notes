version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: my-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js
volumes:
  mongo_data: